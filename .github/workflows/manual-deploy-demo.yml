name: Force Deploy Demo

on:
  workflow_dispatch:
    inputs:
      type:
        description: 'Deployment type'
        required: true
        type: choice
        options:
          - realm
          - registry
          - mundus
        default: realm
      commit:
        description: 'Commit SHA to deploy (leave empty for latest main)'
        required: false
        default: ''
      mode:
        description: 'Deploy mode (upgrade or reinstall)'
        required: false
        type: choice
        options:
          - upgrade
          - reinstall
        default: upgrade
      demo_data:
        description: 'Populate realms with demo/fake data (users, orgs, accounting, etc.)'
        required: false
        type: choice
        options:
          - 'yes'
          - 'no'
        default: 'no'
      test_mode:
        description: 'Enable TEST_MODE umbrella flag (required for any sub-flag)'
        required: false
        type: choice
        options:
          - 'yes'
          - 'no'
        default: 'no'
      test_mode_ii_bypass:
        description: 'TEST_MODE_II_BYPASS: Bypass Internet Identity with deterministic Ed25519 identity'
        required: false
        type: choice
        options:
          - 'yes'
          - 'no'
        default: 'no'
      test_mode_admin_self_registration:
        description: 'TEST_MODE_ADMIN_SELF_REGISTRATION: Allow users to self-register as admins'
        required: false
        type: choice
        options:
          - 'yes'
          - 'no'
        default: 'no'
      test_mode_demo_data:
        description: 'TEST_MODE_DEMO_DATA: Populate realm with demo/fake data'
        required: false
        type: choice
        options:
          - 'yes'
          - 'no'
        default: 'no'
      test_mode_skip_terms:
        description: 'TEST_MODE_SKIP_TERMS: Skip terms acceptance on the join page'
        required: false
        type: choice
        options:
          - 'yes'
          - 'no'
        default: 'no'

jobs:
  deploy-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Print workflow parameters
        run: |
          echo "=== Workflow Parameters ==="
          echo "Repository: ${{ github.repository }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit SHA (github.sha): ${{ github.sha }}"
          echo "Commit SHA (input): ${{ github.event.inputs.commit || '(not set - using github.sha)' }}"
          echo "Effective commit: ${{ github.event.inputs.commit || github.sha }}"
          echo "Deployment type: ${{ github.event.inputs.type }}"
          echo "Deploy mode: ${{ github.event.inputs.mode }}"
          echo "Demo data: ${{ github.event.inputs.demo_data }}"
          echo "Test mode: ${{ github.event.inputs.test_mode }}"
          echo "Test mode II bypass: ${{ github.event.inputs.test_mode_ii_bypass }}"
          echo "Test mode admin self-reg: ${{ github.event.inputs.test_mode_admin_self_registration }}"
          echo "Test mode demo data: ${{ github.event.inputs.test_mode_demo_data }}"
          echo "Test mode skip terms: ${{ github.event.inputs.test_mode_skip_terms }}"
          echo "Network: demo"
          echo "Actor: ${{ github.actor }}"
          echo "Event: ${{ github.event_name }}"
          echo "==========================="

      - name: Checkout code
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.inputs.commit || github.sha }}

      - name: Set deployment commit
        id: commit
        run: |
          DEPLOY_COMMIT="${{ github.event.inputs.commit || github.sha }}"
          echo "deploy_commit=$DEPLOY_COMMIT" >> $GITHUB_OUTPUT
          echo "Deploying commit: $DEPLOY_COMMIT"

      - name: Deploy to demo
        run: |
          # Store identity in a temporary file
          echo "${{ secrets.IC_IDENTITY_PEM }}" > identity.pem
          chmod 600 identity.pem
          
          # Pull the demo image (includes extensions)
          docker pull ghcr.io/${{ github.repository_owner }}/realms:demo-${{ steps.commit.outputs.deploy_commit }}
          
          # Deploy to existing fixed-name canisters on demo network using realms CLI
          docker run --rm \
            -v "$(pwd)/identity.pem:/app/identity.pem" \
            -v "$(pwd)/deploy-logs:/app/deploy-logs" \
            -e DFX_WARNING="-mainnet_plaintext_identity" \
            -e VITE_TEST_MODE=${{ github.event.inputs.test_mode == 'yes' && 'true' || 'false' }} \
            -e VITE_TEST_MODE_II_BYPASS=${{ github.event.inputs.test_mode_ii_bypass == 'yes' && 'true' || 'false' }} \
            -e VITE_TEST_MODE_ADMIN_SELF_REGISTRATION=${{ github.event.inputs.test_mode_admin_self_registration == 'yes' && 'true' || 'false' }} \
            -e VITE_TEST_MODE_DEMO_DATA=${{ github.event.inputs.test_mode_demo_data == 'yes' && 'true' || 'false' }} \
            -e VITE_TEST_MODE_SKIP_TERMS=${{ github.event.inputs.test_mode_skip_terms == 'yes' && 'true' || 'false' }} \
            ghcr.io/${{ github.repository_owner }}/realms:demo-${{ steps.commit.outputs.deploy_commit }} \
            bash -c "(dfx identity import my-identity /app/identity.pem --storage-mode=plaintext --force && \
                     dfx identity use my-identity && \
                     bash scripts/manual-deploy-staging.sh '${{ github.event.inputs.type }}' '${{ github.event.inputs.mode }}' '${{ github.event.inputs.demo_data }}' 'demo'); \
                     EXIT_CODE=\$?; \
                     mkdir -p /app/deploy-logs && \
                     cp -f dfx.log /app/deploy-logs/ 2>/dev/null || true && \
                     cp -f dfx2.log /app/deploy-logs/ 2>/dev/null || true && \
                     cp -f realms.log /app/deploy-logs/ 2>/dev/null || true; \
                     exit \$EXIT_CODE"
          
          # Clean up
          rm identity.pem
      
      - name: Upload deployment logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: force-deploy-demo-logs
          path: deploy-logs/
          retention-days: 7
