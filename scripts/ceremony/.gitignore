# Ceremony workspace — never commit outputs
vm/cache/
artifacts/
artifacts/private/
secrets/
sim-yubikeys/
.operator-credentials.txt
# Real operator PIV PIN/PUK files — only *.example may be committed
my-piv.txt
operator-credentials.txt
*-credentials.txt
*.pem
# Local ceremony layout — copy from ceremony-config.example.json; do not commit
ceremony-config.json
# Bundled dfx binary (~150 MB) — fetch on the prep machine, do not commit
bin/dfx
bin/dfx-*
*.tar.gz
