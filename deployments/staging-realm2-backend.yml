# Unified deployment descriptor — Backend hotfix for Agora (realm2) on staging
# Usage: realms deploy --file deployments/staging-realm2-backend.yml
# See: https://github.com/smart-social-contracts/realms/issues/160
version: 1

object:
  type: realm
  subtypes: backend
  quarters: 2
  manifest: ./examples/demo/realm2/manifest.json
  core_codebase:
    path: ./src
  extensions_codebase:
    path: none
  codices_codebase:
    path: none
  network: staging
  id_in_registry: 2
  mode: upgrade
  parameters: {}
